<% unless curriculum.nil? || CurriculumAffiliation
  .find_by(curriculum:, learning_unit:).nil?%>
  <%= "Go back to the " %> <%= link_to "#{curriculum.name}'s curriculum",
    curriculum_path(curriculum) %>
  <br><br>
<% end %>